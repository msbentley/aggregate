from setuptools import setup

setup(name='aggregate',
      version='0.1',
      description='Particle aggregation simulation',
      url='https://github.com/msbentley/aggregate',
      author='Mark S. Bentley',
      author_email='mark@lunartech.org',
      packages=['aggregate'],
      zip_safe=False)
